/*jslint node: true */
/*globals describe, it */
var expect = require('chai').expect;
var ps = require('../lib/parse-ps');

describe('dpdf2html', function () {
    describe('parse', function () {
        it('should work', function () {
            expect(2).to.equal(2);
        });
    });
});
